[package]
name = "vell-mode"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"

[features]
test-support = []

[dependencies]
vell-core = { path = "../vell-core" }
vell-protocol = { path = "../vell-protocol" }
tokio-util = { version = "0.7", features = ["rt"] }
